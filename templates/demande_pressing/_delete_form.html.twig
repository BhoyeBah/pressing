<form method="post" action="{{ path('app_demande_pressing_delete', {'id': demande_pressing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_pressing.id) }}">
    <button class="btn">Delete</button>
</form>
